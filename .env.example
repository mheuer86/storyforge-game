# Anthropic API key (server-side, for demo mode)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: access code for demo mode gate (leave empty for open access)
ACCESS_CODE=

# Optional: override the default model
# STORYFORGE_MODEL=claude-sonnet-4-6
