> *This was generated by AI during triage.*

# New pressure threads are created without the reveal clues that caused them

Category: bug
Status: ready-for-human
Type: AFK
Area: SF2 / thread creation / clue attachment
Reporter: Martin
Observed: 2026-05-04

## Observation

Two new threads appeared without the load-bearing clues that motivated them:

- Turn 12 revealed/accepted the data-core job, Veyne wants the core back, and the destination is a dead-drop. `Deliver the Data Core to Dead-Drop` appeared with no attached clues at first.
- Turn 17 revealed `Sev Anrath - Veyne Logistics` and `DEPARTURE MONITOR: ACTIVE`. `Evade Veyne Logistics Tracking` appeared with no attached clues.

Both threads later gained some state, but the initial creation was empty at the moment the player most needed durable context.

## Evidence

- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/chapter-01-turn-12.txt`
- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/chapter-01-turn-17.txt`
- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/observations-partial.json`

## Expected

When a thread is created from a narrated reveal, at least one current-turn clue should be attached immediately, unless the thread is explicitly speculative and marked that way.

## Likely ownership

- Primary: Archivist thread creation and clue attachment.
- Secondary: validation should reject or flag active pressure/spine threads created with no anchors/clues when the current turn contains eligible reveal facts.

## Acceptance criteria

- [x] Creating a new thread from a current-turn reveal attaches the reveal fact as a clue in the same turn.
- [x] Empty active pressure/spine threads are rejected or flagged unless explicitly allowed.
- [x] A replay fixture covers a minimized data-core/cargo pressure creation path.

## Blocked by

None - can start immediately.

## Comments

> *This was generated by AI during triage.*

### Triage - 2026-05-04

Decision: ready-for-agent. This is a clean SF2 state-ownership bug: active pressure created from a reveal needs an immediate durable anchor, otherwise the thread name becomes the only state carrier.

### Agent Brief

Category: bug

Summary: Attach current-turn reveal clues when creating active pressure threads.

Current behavior: New threads can appear after a major reveal without any attached clue that explains why they exist. The missing attachment is most visible when a data-core job or active departure monitor creates pressure, but the thread begins empty.

Desired behavior: If a current-turn reveal motivates a new active pressure or spine thread, that same turn should attach at least one relevant clue, document, or reveal fact. Empty active threads should be rejected, repaired, or explicitly marked as speculative/deferred when no current clue is eligible.

Key interfaces: thread creation patch contract, clue creation and attachment, validation drift reporting, thread retrieval packets, and replay fixtures for current-turn reveal anchoring.

Acceptance criteria: Creating the data-core and departure-monitor threads attaches their motivating facts in the same turn; empty active pressure/spine threads are rejected or flagged unless explicitly allowed; the fix is covered by a focused replay fixture.

Out of scope: Requiring every background/worldbuilding thread to have clues, changing Author-created chapter setup threads, or adding a player-visible speculative-thread taxonomy unless the existing state model needs it.
