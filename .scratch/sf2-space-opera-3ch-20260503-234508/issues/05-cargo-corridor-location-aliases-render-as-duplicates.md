> *This was generated by AI during triage.*

# Cargo corridor location aliases render as duplicate locations

Category: bug
Status: ready-for-agent
Type: AFK
Area: SF2 / location normalization / UI rendering
Reporter: Martin
Observed: 2026-05-04

## Observation

Moving from the kiosk to the cargo-side corridor produced inconsistent location names for the same place:

- `HARROW SPUR DOCKING ARM4 CORRIDOR`
- `harrow spur docking arm4 corridor`
- `Harrow Spur - Docking Arm 4, Cargo-Side Corridor`

The locations rail then rendered multiple semantically equivalent corridor entries.

## Evidence

- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/chapter-01-turn-08.txt`
- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/chapter-01-turn-18.txt`
- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/observations-partial.json`

## Expected

Location aliases that differ only by case, punctuation, spacing, or generated ID should merge into one canonical location before display and retrieval.

## Likely ownership

- Primary: location patch application / canonicalization.
- Secondary: `components/sf2/play-shell.tsx` location rail can defensively dedupe exact semantic duplicates.

## Acceptance criteria

- [ ] The corridor renders as one player-facing location after scene movement.
- [ ] Alias normalization handles case, punctuation, and `Arm4` vs `Arm 4`.
- [ ] A replay fixture covers this exact alias family or a minimized equivalent.

## Blocked by

None - can start immediately.

## Comments

This resembles the earlier duplicate-opening-locations class, but the observed alias variation is more aggressive than exact duplicate display text.

> *This was generated by AI during triage.*

### Triage - 2026-05-04

Decision: ready-for-agent. The reproduction is concrete, and the expected behavior fits SF2's existing location-normalization responsibility. This should be fixed at canonicalization/merge time, with UI display dedupe only as a defensive layer.

### Agent Brief

Category: bug

Summary: Merge semantically equivalent cargo-corridor aliases before location display and retrieval.

Current behavior: Case, punctuation, and spacing variants of the same cargo-side corridor are persisted and displayed as separate locations. That creates duplicate navigation state for a single fictional place.

Desired behavior: Location aliases that differ only by formatting or minor generated phrasing should resolve to one canonical location, with references moved to the surviving entity.

Key interfaces: location semantic keys, location merge/reference replacement, scene movement writes, scene snapshot writes, location rail display dedupe, and replay fixtures for location normalization.

Acceptance criteria: The observed corridor alias family renders as one location; `Arm4` and `Arm 4` normalize together; case and punctuation variants merge; references to merged aliases still point at the canonical location; a focused replay fixture covers the minimized alias case.

Out of scope: Changing map UX, inventing a full geocoder, or merging locations that are fictionally distinct despite similar labels.
