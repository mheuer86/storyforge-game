> *This was generated by AI during triage.*

# SF2 diagnostics exports are unavailable in the in-app browser

Category: bug
Status: ready-for-human
Type: AFK
Area: SF2 / diagnostics export / playtest workflow
Reporter: Martin
Observed: 2026-05-04

## Observation

During the Space Opera SF2 playthrough, the diagnostics panel exposed "Download session log" and "Download replay fixture" buttons, but the Codex in-app browser did not produce downloadable files. Manual checks found no current `sf2-session-*.json` or `sf2-replay-*.json` files in `~/Downloads`, and the only recent-ish session file was an unrelated earlier one-turn `Wrench` campaign.

This blocked the intended after-each-chapter workflow: play chapter, export debug log, export replay fixture, analyze.

## Evidence

- User confirmed: "Seeing the same behavior, no downloads from the in app browser possible. So we can't use them..."
- The current run has text snapshots only under `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/`.
- Repeated file checks found no current session/replay JSON exports for the `Codex` run.

## Expected

SF2 playtest diagnostics should have an export path that works in the Codex in-app browser, or a fallback export path that does not depend on browser downloads.

## Likely ownership

- Primary: `app/play/v2/page.tsx` diagnostics export handlers.
- UI surface: `components/sf2/play-shell.tsx` diagnostics panel.
- Product/workflow: SF2 regression fixture workflow.

## Acceptance criteria

- [x] A playtester can export/capture the current session log from the in-app browser without relying on native download support.
- [x] A playtester can export/capture the current replay fixture from the in-app browser without relying on native download support.
- [x] Exported artifacts preserve the same JSON shape currently produced by `downloadSessionLog()` and `downloadReplayFixture()`.
- [x] The workflow is documented wherever SF2 playtest/regression fixture instructions live.

## Blocked by

None - can start immediately.

## Comments

Potential implementation options: copy JSON to clipboard, render JSON in a modal with save/copy affordance, write to local app storage for later retrieval, or expose a localhost-only debug endpoint gated to development mode.

> *This was generated by AI during triage.*

### Triage - 2026-05-04

Decision: ready-for-agent. This is a workflow-blocking diagnostics bug with clear reproduction evidence and no missing product decision; keep the fix conservative by adding a fallback while preserving the current export JSON shapes.

### Agent Brief

Category: bug

Summary: Provide an SF2 diagnostics export path that works when the browser cannot complete native file downloads.

Current behavior: The diagnostics actions build the right session-log and replay-fixture payloads, but the Codex in-app browser does not save the generated files. A playtester can finish a chapter and still be unable to capture the artifacts needed for replay analysis.

Desired behavior: Session logs and replay fixtures remain available through at least one non-download fallback in the playtest surface. Existing download behavior may stay, but failure or unsupported-download environments should still let the user copy, view, or otherwise capture the same JSON payload.

Key interfaces: diagnostics export payload builders, diagnostics panel actions, browser clipboard/view fallback behavior, and SF2 playtest workflow documentation.

Acceptance criteria: A playtester can capture both session logs and replay fixtures from the in-app browser; fallback artifacts preserve the same JSON shape as the current downloads; the UI reports success or failure clearly; the documented playtest workflow names the fallback.

Out of scope: Adding a production persistence service, changing the replay/session schema, or replacing working native downloads where they are supported.

### Fix Note - 2026-05-04

Implemented a local fallback in `app/play/v2/page.tsx` and `components/sf2/play-shell.tsx`: diagnostics now keep the existing download buttons and add "Copy session JSON" / "Copy replay JSON" buttons that copy the same payloads to the clipboard with a visible success/failure message. `app/design/play/page.tsx` was updated with no-op props for the design preview. `AGENTS.md` now documents the in-app-browser copy fallback in the SF2 regression fixture workflow.

Verification: `npm run build` passes.
