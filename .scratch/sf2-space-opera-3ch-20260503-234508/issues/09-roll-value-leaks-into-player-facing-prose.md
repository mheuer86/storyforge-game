> *This was generated by AI during triage.*

# Roll value leaks into player-facing prose

Category: bug
Status: ready-for-human
Type: AFK
Area: SF2 / Narrator / display sentinel
Reporter: Martin
Observed: 2026-05-04

## Observation

The Turn 18 success prose included roll-result commentary in the narrative body:

- `19. Clean.`
- `Nineteen. Clean.`

This reads like mechanical/debug narration rather than in-world prose. It also appears to have been caught by a display sentinel observation for `roll_value_leak`, but the prose still displayed it.

## Evidence

- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/chapter-01-natural-close-turn-18.txt:325`
- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/chapter-01-natural-close-turn-18.txt:593`

## Expected

Roll result values should stay in the mechanical roll UI/log, not the player-facing narrative prose, unless explicitly styled as a mechanic outside the prose body.

## Likely ownership

- Primary: Narrator prompt / roll-result continuation instructions.
- Secondary: display sentinel enforcement if `roll_value_leak` should block or repair rather than merely observe/quarantine writes.

## Acceptance criteria

- [x] Narrator roll continuations do not include raw roll values like `19. Clean.` in prose.
- [x] Existing display sentinel behavior for `roll_value_leak` is clarified: repair on live stream, observe in replay.
- [x] A replay fixture or narrator fixture covers roll-value leakage in success prose.

## Blocked by

None - can start immediately.

## Comments

> *This was generated by AI during triage.*

### Triage - 2026-05-04

Decision: ready-for-agent. The display sentinel already recognizes the failure class, so the remaining work is to make the role contract and enforcement path agree on whether roll-value leaks are blocked, repaired, or only observed.

### Agent Brief

Category: bug

Summary: Prevent raw roll values from appearing in player-facing narrative prose.

Current behavior: Roll continuation prose can include explicit roll-value phrasing such as `19. Clean.` even though the roll value belongs in mechanical UI/log surfaces. A sentinel observation may record the leak while still allowing the prose to display.

Desired behavior: Narrator prose should express outcomes fictionally without restating raw roll values. Roll values remain available in the mechanical roll UI/log. If the display sentinel detects a roll-value leak, the configured action should prevent the leaked prose from reaching the player or repair it before display.

Key interfaces: roll-result continuation context, narrator role instructions, display sentinel scan results, repair/block behavior, client handling of sentinel telemetry, and narrator or replay fixtures.

Acceptance criteria: Roll continuations do not include raw values in prose; sentinel behavior for roll-value leaks is explicit and enforced consistently; a fixture covers the observed `19. Clean.` style leak; mechanical roll result display outside prose remains unchanged.

Out of scope: Changing dice mechanics, hiding roll results from the mechanical UI, or banning all numeric values that are legitimate in-world facts.
