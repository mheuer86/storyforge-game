> *This was generated by AI during triage.*

# Hold thread resolves before the clamp release is processed

Category: bug
Status: fixed-locally
Type: AFK
Area: SF2 / thread resolution / pacing state
Reporter: Martin
Observed: 2026-05-04

## Observation

After Lira explained the release chip at Turn 13, but before the player returned to Coll or processed the chip, the visible `Clear the Forty-Thousand Hold` spine disappeared or was heavily decremented. This made the hold look resolved before the fiction actually cleared it.

The release was not processed until Turn 16, when Coll accepted the chip and the mechanical clamp release sounded.

## Evidence

- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/chapter-01-turn-13.txt`
- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/chapter-01-turn-16.txt`
- `/Users/martin.heuer/storyforge/.scratch/sf2-space-opera-3ch-20260503-234508/artifacts/observations-partial.json`

## Expected

The hold-clearance thread should remain active until the release chip is actually processed and the clamp release is confirmed in fiction/state.

## Likely ownership

- Primary: thread tension/resolution updates in Archivist writes.
- Secondary: close-readiness pressure calculations if they treat "solution obtained" as equivalent to "problem resolved."

## Acceptance criteria

- [x] Receiving a release mechanism does not resolve the hold-clearance thread by itself.
- [x] Processing the release chip and hearing/confirming clamp release resolves or transitions the thread.
- [x] A replay fixture covers "release chip obtained" as distinct from release processed.

## Blocked by

None - can start immediately.

## Comments

> *This was generated by AI during triage.*

### Triage - 2026-05-04

Decision: ready-for-agent. This is a resolution-gating bug with a clear desired distinction: acquiring the means to solve a problem is not the same state transition as solving it.

### Agent Brief

Category: bug

Summary: Keep the hold-clearance thread active until the clamp release is actually processed.

Current behavior: The hold thread can disappear or lose too much pressure after the player learns about or receives the release mechanism, before the fiction confirms that the clamp has been released.

Desired behavior: The system should distinguish "solution obtained" from "problem resolved." Receiving or understanding the release chip can change the thread's known solution or lower uncertainty, but the thread should resolve only when the release is processed and confirmed in fiction/state.

Key interfaces: thread status transitions, tension/pressure updates, resolution criteria in Archivist writes, close-readiness calculations, and replay fixtures for staged resolution.

Acceptance criteria: Receiving a release mechanism does not resolve the hold-clearance thread by itself; processing the release and confirming clamp release resolves or transitions the thread; close-readiness calculations do not treat the pre-processing state as fully resolved; a replay fixture covers both states.

Out of scope: Changing chapter length targets, weakening legitimate partial progress updates, or making all threads require an explicit confirmation scene.
