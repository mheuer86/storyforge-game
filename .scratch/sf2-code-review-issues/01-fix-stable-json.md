# Fix `stableJson` to use structural equality

Status: ready-for-agent
Category: bug
**Type:** AFK
**Source:** sf2 code review pass 1, finding #3

## What to build

`turn-resolution/resolve.ts:255-257` defines `stableJson` as `JSON.stringify(value ?? null)`. The name lies — `JSON.stringify` is not stable across object key insertion orders. When `applyArchivistPatch` rebuilds a thread with different key ordering than the prior thread, `hasDurableTargetMutation` returns false positives or negatives based on key order alone.

Replace the function with a structurally-stable comparison: either a stable-stringify library (`fast-json-stable-stringify`, `json-stable-stringify`) or a small structural-equality helper. Update the call sites in `applyTurnResolutionConsequences`.

## Acceptance criteria

- [ ] `stableJson` (or its replacement) returns identical output for objects whose keys are inserted in different orders
- [ ] A test exercises the bug: build two thread objects with the same field values but different key insertion order; assert `hasDurableTargetMutation` returns `false` (no mutation observed)
- [ ] No regression in existing turn-resolution behavior on the playthrough fixtures
- [ ] If a library is added, it's a dev-only or runtime-light dependency

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** bug
**Summary:** Make durable mutation detection insensitive to object key insertion order.

**Current behavior:** Turn resolution compares state slices with order-sensitive JSON serialization.

**Desired behavior:** Semantically equal state slices compare equal regardless of key insertion order, while real target mutations still register.

**Key interfaces:** `applyTurnResolutionConsequences()` and the helper used by durable target mutation detection.

**Acceptance criteria:** Complete the issue checklist and run the focused replay fixture plus the SF2 replay suite.

**Out of scope:** Broader turn-resolution policy changes.
