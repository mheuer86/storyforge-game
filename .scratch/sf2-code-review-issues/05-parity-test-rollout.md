# Extend parity test to remaining roles

Status: ready-for-agent
Category: enhancement
**Type:** AFK
**Source:** sf2 code review pass 2, finding A2

## What to build

Apply the parity-test pattern designed in issue #4 to the remaining roles:
- **Narrator** — `narrator/tools.ts` vs `narrator/prompt.ts`
- **Author** — `author/tools.ts` vs `author/prompt.ts`
- **Arc-author** — `arc-author/tools.ts` vs `arc-author/prompt.ts`
- **Chapter-meaning** — `chapter-meaning/tools.ts` vs `chapter-meaning/prompt.ts`

Mostly mechanical once #4 lands. Each role gets its own parity test using the representation chosen in #4.

Expected drifts to surface (from review):
- Narrator: `mechanical_effects[].kind` enum vs the firewall ALLOWED set (`combat` listed in firewall, missing in tool)
- Author: prompt rules 19-20 (1-2 NPCs visible at opening; withheld_premise_facts) — currently no schema enforcement at all
- Arc-author: should be cleanest (smallest gap)
- Chapter-meaning: should be cleanest (compact schema)

For each role, reconcile any drift surfaced.

## Acceptance criteria

- [ ] Parity test file per role
- [ ] Each test runs in CI / local test command
- [ ] All drifts surfaced are either reconciled (schema or prompt change) or explicitly waived with a comment explaining why
- [ ] Cross-role reconciliation: firewall ALLOWED sets derived from (or asserted equal to) the tool schema enums for narrator/archivist/author

## Blocked by

- Issue #4 (Archivist parity test prototype)

## Comments

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Apply the parity-test pattern to all remaining SF2 roles.

**Current behavior:** Cross-role prompt/schema/firewall drift is reviewed manually.

**Desired behavior:** Each role has an automated parity check or an explicit waiver for intentional mismatch.

**Key interfaces:** Role prompt contract metadata, role tool schemas, actor firewall allowed-write contracts.

**Acceptance criteria:** Complete the issue checklist after issue #4 lands.

**Out of scope:** Major prompt rewrites beyond reconciling drift the tests expose.
