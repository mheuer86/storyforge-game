# SF2 Narrative Quality Status Reconciliation

Verified against code and fixtures on 2026-05-08.

| Ticket | Verdict | Evidence | Fixture / command | Follow-up |
|---|---|---|---|---|
| 01 v1-style genre injection | partial | Genre-specific examples are injected through `buildAuthorRole(genreId)` and `buildNarratorRole(genreId)` via `getSf2GenreExamples` in `lib/sf2/author/prompt.ts:18`, `lib/sf2/narrator/prompt.ts:376`, and `lib/sf2/genre-examples.ts:1`. This is real for examples, but not a full V1-style genre prompt parity layer. | `rg "buildAuthorRole|buildNarratorRole|genreExamples" lib/sf2` | Keep open only if the intended scope is full V1 parity; otherwise mark built for example injection. |
| 02 human outcome spectrum | verified built | Author prompt requires human consequences in outcome spectrum at `lib/sf2/author/prompt.ts:119`; raw and normalized validators reject non-human/procedural-only outcomes via `hasHumanOutcomeAnchor` in `lib/sf2/author/contract.ts:53`, `lib/sf2/author/contract.ts:495`, and `lib/sf2/author/contract.ts:655`. | `npm run sf2:replay -- fixtures/sf2/replay/author-outcome-spectrum-rejects-procedural-only.json` | Close as shipped. |
| 03 genre-neutral trigger examples | verified built | Trigger examples come from `getSf2GenreExamples`, with generic examples and per-genre substitutions in `lib/sf2/genre-examples.ts:10`; Author prompt uses those examples instead of hard-coded single-world examples at `lib/sf2/author/prompt.ts:58`, `lib/sf2/author/prompt.ts:63`, and `lib/sf2/author/prompt.ts:65`. | `rg "entityBoundTriggers|sceneCoupledTriggers|pcMisfitChapterRule" lib/sf2` | Close as shipped, unless ticket requires adding more genre variants. |
| 04 continuation stakes, not procedure | verified built | Continuation law requires stakes escalation and `continuation_dramatic_turn` at `lib/sf2/author/prompt.ts:72`; procedure gravity prompt guidance is at `lib/sf2/author/prompt.ts:341`; validators enforce continuation dramatic turn and procedure ownership/max opening beats in `lib/sf2/author/contract.ts:745`, `lib/sf2/author/contract.ts:779`, and `lib/sf2/author/contract.ts:814`. | `npm run sf2:replay -- fixtures/sf2/replay/author-continuation-procedure-gravity-rejects-unowned-mechanism.json`; `npm run sf2:replay -- fixtures/sf2/replay/author-continuation-procedure-gravity-accepts-owned-leverage.json` | Close as shipped. |
| 05 human_stakes | verified built | Tool schema includes `human_stakes` in `lib/sf2/author/tools.ts:708`; prompt requires exactly 2-3 stakes at `lib/sf2/author/prompt.ts:120` and `lib/sf2/author/prompt.ts:130`; normalized setup carries `humanStakes` in `lib/sf2/author/contract.ts:398`; raw and normalized validators enforce count, valid payer, valid cost surface, and triggering pressure in `lib/sf2/author/contract.ts:674` and `lib/sf2/author/contract.ts:714`; Narrator roll continuation uses matching stakes in `lib/sf2/narrator/turn-context.ts:265`. | `rg "human_stakes|humanStakes" lib/sf2`; full suite `npm run sf2:replay -- fixtures/sf2/replay` | Close as shipped; add a dedicated negative fixture only if stricter stake semantics are desired. |
| 06 mechanism vocabulary trim | partial | Procedure gravity vocabulary exists in code as regexes in `lib/sf2/author/contract.ts:25`; prompt now frames mechanisms as leverage rather than story shape in `lib/sf2/author/prompt.ts:341`; narrator bible says procedure should be scenery/leverage at `lib/sf2/narrator/prompt.ts:185`. The old vocabulary list is not obviously removed everywhere; it is constrained by validation rather than fully trimmed out. | `rg "procedure_budget|procedure|mechanism|timer|queue|audit" lib/sf2/author lib/sf2/narrator` | Do a narrow prompt-copy pass if the ticket meant literal wording cleanup rather than behavioral enforcement. |
| 07 v2 continuation vs cherry-pick | open design work | No code decision verified here; user asked to leave this open as a strategic decision after playtesting. | Not run. | Keep open until after playtest reconciliation. |
| 08 pressure glossary | verified built | Narrator role includes a named `Pressure manifestation` glossary/discipline section at `lib/sf2/narrator/prompt.ts:462`; failed-roll private pressure injection in `lib/sf2/narrator/turn-context.ts:269` uses the same vocabulary and ties it to `human_stakes`. | `rg "Pressure manifestation|Private roll pressure manifestation|human_stakes" lib/sf2` | Close as shipped, unless the desired glossary is a separate docs artifact. |
| 09 scene source of truth | open design work | Not reconciled as implementation because user asked to leave it open and merge into Domain 18. Existing scene systems are broad and need a separate domain-model pass. | Not run. | Merge into Domain 18 rather than closing here. |
| 10 diagnostic finding envelope | verified built | `Sf2DiagnosticFinding` is typed in `lib/sf2/types.ts:618`; adapter functions convert display sentinel, coherence, invariant, archivist, and pending findings in `lib/sf2/diagnostics.ts:11`; open error query exists at `lib/sf2/diagnostics.ts:74`; store uses the envelope in `lib/sf2/diagnostics-store.ts:45`. | `npm run sf2:replay -- fixtures/sf2/replay/diagnostic-envelope-adapts-cross-source-findings.json` | Close as shipped. |
| 11 prompt compression/V1 parity audit | open design work | User asked to defer this until after status reconciliation to avoid auditing stale tickets. | Not run. | Do after closing or re-scoping 01-10. |

## Verification

- `npm run sf2:replay -- fixtures/sf2/replay/author-outcome-spectrum-rejects-procedural-only.json` passed.
- `npm run sf2:replay -- fixtures/sf2/replay/author-continuation-procedure-gravity-rejects-unowned-mechanism.json` passed.
- `npm run sf2:replay -- fixtures/sf2/replay/author-continuation-procedure-gravity-accepts-owned-leverage.json` passed.
- `npm run sf2:replay -- fixtures/sf2/replay/diagnostic-envelope-adapts-cross-source-findings.json` passed.
- `npm run sf2:replay -- fixtures/sf2/replay` passed: 164/164 fixtures.
