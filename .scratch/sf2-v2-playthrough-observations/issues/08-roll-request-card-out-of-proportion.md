> *This was generated by AI during triage.*

# Roll request card feels oversized and visually off

Category: enhancement
Status: needs-triage
Area: SF2 / V2 UI / roll request styling
Reporter: Martin
Observed: 2026-05-02
Evidence: `/Users/martin.heuer/Library/Application Support/CleanShot/media/media_8fQ1VtcWTl/CleanShot 2026-05-02 at 19.51.11@2x.png`

## Observation

The roll request element feels out of proportion compared to the rest of the V2 UI. The fonts are too large, the card is visually heavy, and the styling still feels off. A primary-shade background may fit the UI better.

In the screenshot, the `Insight Check` card dominates the lower center pane relative to narrator text, quick actions, and rail panels.

## Expected

The roll request should feel integrated with the V2 interface: compact, readable, and mechanically clear without overpowering the narrative pane. Idle roll requests may work better with a restrained primary-tinted background.

## Lightweight Code Note

No fix attempted. Initial inspection points at `components/sf2/play-shell.tsx`:

- `DiceTray` renders `RollCardView` inside `max-w-[820px]`.
- `RollCardView` uses large idle title text: `text-[24px] ... md:text-[30px]`.
- `RollValueBox` uses `h-20 w-20 ... md:h-24 md:w-24`.
- The idle card uses `sf2-roll-idle-card border border-primary/55`, but the background treatment is delegated to CSS rather than clearly using a compact primary shade in the component.

## Triage Questions

- Should pending roll cards be visually smaller than resolved roll history cards, or should both share a compact style?
- Should the idle roll request background use `bg-primary/10` or another genre-tokenized primary shade?
- Should the dice/DC boxes shrink, or should only the typography and padding be reduced?

## Comments
