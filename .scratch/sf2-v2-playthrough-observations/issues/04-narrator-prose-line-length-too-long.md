> *This was generated by AI during triage.*

# Narrator prose line length is too long on desktop

Category: enhancement
Status: needs-triage
Area: SF2 / V2 UI / narrative readability
Reporter: Martin
Observed: 2026-05-02
Evidence: `/Users/martin.heuer/Library/Application Support/CleanShot/media/media_OqLFDsEAit/CleanShot 2026-05-02 at 19.44.00@2x.png`

## Observation

In the V2 desktop layout, narrator prose spans a long line length. The current measure makes the story text feel less compact and harder to read than it should.

The screenshot shows the narrator text filling most of the center column, with long prose lines across the main panel.

## Expected

Narrator prose should have a more compact reading measure on desktop, improving readability without making the whole play surface feel cramped.

## Lightweight Code Note

No fix attempted. Initial inspection points at `components/sf2/play-shell.tsx`:

- `TurnStream` uses `max-w-[940px]` for the narrative column.
- `GMMessage` fills the available stream width and does not apply a separate readable text measure.
- The main V2 grid allows a wide center column on desktop, so the prose line length stretches with the play surface.

## Triage Questions

- Should only narrator prose be narrowed, while player messages and action controls keep the current width?
- What is the target desktop reading measure: approximately 65-80 characters, or a slightly wider cinematic text block?
- Should the prose block stay left-aligned inside the center pane, or sit in a narrower centered column?

## Comments
